@start-cpd "Minimal"

rootPackage Minimal

component testComp {
	component first_comp{
		require first_ifaces
	}
	interface first_iface {
		someName : String
		someOperation(anObject : AbstractClass, aPrimitive : int) : long
	}
}
interface second_iface {
	someName : String
	someOperation(anObject : AbstractClass, aPrimitive : int) : long
}
isa (testComp, second_iface)
abs (testComp, second_iface)

@end-cpd